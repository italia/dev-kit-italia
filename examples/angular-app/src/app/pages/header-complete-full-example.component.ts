import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';

@Component({
  selector: 'header-complete-full-example-page',
  templateUrl: './header-complete-full-example.component.html',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
})
export class HeaderCompleteFullExampleComponent {}
