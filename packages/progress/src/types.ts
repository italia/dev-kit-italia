/**
 * Main type of progress indicator.
 */
export type ProgressType = 'bar' | 'donut' | 'spinner';

/**
 * Variant for the progress bar color
 */
export type ProgressBarColor = 'default' | 'success' | 'info' | 'warning' | 'danger';

/**
 * Size variants for the progress spinner
 */
export type ProgressSpinnerSize = 'sm' | 'md' | 'lg' | 'xl';
