import { Meta, Story, Canvas, Controls, Description, Subtitle } from '@storybook/addon-docs/blocks';
import * as HeroStories from './it-hero.stories';

<Meta of={HeroStories} />

# Hero

<description>Sezione a tutta larghezza per mettere in evidenza combinazioni di contenuti testuali, immagini e pulsanti.</description>


## Cosa fa

Il componente Hero (`it-hero`) è una sezione a tutta larghezza che mette in evidenza contenuti in apertura di pagina con un grande impatto visivo, per invogliare gli utenti a continuare la navigazione.

[Approfondisci quando e come usare il componente Hero](https://designers.italia.it/design-system/componenti/hero/)


## Anteprima e attributi del componente

Modifica gli attributi nella tabella per personalizzare in tempo reale l’aspetto e il comportamento del componente. Per vedere come cambia il codice, clicca su <strong>Show code</strong>.

<Canvas of={HeroStories.EsempioInterattivo} />
<Controls of={HeroStories.EsempioInterattivo} />

## Indicazioni generali

Considera un hero come una copertina o un banner molto in evidenza:

- non usare più hero sulla stessa pagina e moderane l'uso all'interno dello stesso sito o servizio;
- puoi estendere la larghezza del componente Hero all’intera finestra, per mostrare messaggi in evidenza sul tuo sito;
- quando usi hero con approccio "fullwidth", sfrutta tutta la larghezza della viewport;
- se sono presenti call to action, usa la corretta semantica HTML (`<a>` per link di navigazione o ancore, `<it-button>` per azioni).


## Accessibilità

- Se includi contenuti testuali, usa il corretto livello `h` per il titolo (`h1`-`h6`), in base alla gerarchia dei contenuti della pagina.  Il componente calcolerà automaticamente l'attributo `aria-labelledby` facendolo puntare al primo heading disponibile.
- Se non è presente alcun heading nel testo del componente, i lettori di schermo annunceranno la sezione in modo generico, senza ulteriori dettagli. In questo caso, oppure se usi la variante con sola immagine, imposta sempre l'attributo `it-aria-label` sul tag `<it-hero>` per fornire un'etichetta descrittiva.
- Se usi la variante con testo e immagine di sfondo, verifica che ci sia sempre contrasto adeguato tra testo e sfondo.
- Valuta sempre con attenzione l'opportunità di usare più Hero nella stessa pagina.

## Con immagine
Per mostrare una immagine di sfondo nel componente `it-hero`, inserisci l'immagine nello slot `background` dedicato.

Ricordati di impostare l'attributo `it-aria-label` sul tag `<it-hero>` per indicare ai lettori di schermo di cosa si tratta.

<Canvas of={HeroStories.ConImmagine} />

## Con contenuti testuali
Per aggiungere contenuti testuali, inserisci il testo nello slot `text` dedicato.
<div class="callout callout-success">
<div class="callout-inner mt-0">
<div class="callout-title">
<span class="text">Accessibilità dei contenuti testuali</span>
</div>
<p>
Usa il corretto livello `h` per il titolo (`h1`-`h6`), in base alla gerarchia dei contenuti su pagina.
</p>
</div>
</div>

<Canvas of={HeroStories.ConContenutiTestuali} />

### Con contenuti testuali centrati
Per coentrare i testi orizzontalmente, aggiungi l'attributo `center` al tag `<it-hero>`.

<Canvas of={HeroStories.ConContenutiTestualiCentrati} />

## Con testi ed immagine di sfondo

Per visualizzare i testi in overlay sulle immagini, viene creato automaticamente un div scuro in opacità per migliorare la leggibilità del testo in caso di immagini dai toni chiari.

<div class="callout callout-success">
<div class="callout-inner mt-0">
<div class="callout-title">
<span class="text">Accessibilità e contrasto</span>
</div>
<p>
Verifica che ci sia sempre contrasto adeguato tra testo e sfondo.
</p>
</div>
</div>

<Canvas of={HeroStories.ConTestoEImmagine} />

### Con overlay di colore primario
Per un div opaco di colore primario, aggiungi l'attributo `overlay-color="primary"` al tag `<it-hero>`.

<Canvas of={HeroStories.ConTestoEImmaginePrimary} />


### Con overlay e filtro di colore primario
Per aggiungere un filtro di tipo “screen”, aggiungi l'attributo `overlay-color="filter"` al tag `<it-hero>`.

<Canvas of={HeroStories.ConTestoEImmaginePrimaryFilter} />


## Con immagine e margine negativo per contenuti sovrapposti
In alcuni casi, il contenuto che segue può sovrapporsi al componente Hero.

Per far sovrappore al margine inferiore dell'hero il contenuto che segue, aggiungi l'attributo `overlap` al tag `<it-hero>`. 

Nell'esempio viene mostrata la sovrapposizione con una card.

<Canvas of={HeroStories.OverlapContent} />

## Personalizzazione degli stili

Per personalizzare gli stili, usa i selettori `::part` dedicati:

- `::part(img-responsive-wrapper)`, per il wrapper dell'immagine di sfondo;
- `::part(text-container)`, per il contenitore dello slot del testo.

Puoi cambiare il valore delle seguenti variabili CSS per:
<table>
<thead>
<tr>
<td>Variabile CSS</td>
<td>Descrizione </td>
<td>Predefinito</td>
</tr>
</thead>
<tbody>
<tr>
<td>`--#${prefix}hero-background-color`</td>
<td>per cambiare il colore di sfondo dell'hero </td>
<td>`var(--#${prefix}color-background-primary)`</td>
</tr>
<tr>
<td>`--#${prefix}hero-car-size`</td>
<td></td>
<td>`1rem`</td>
</tr>
<tr>
<td>`--#${prefix}hero-height`</td>
<td>per cambiare l'altezza dell'hero </td>
<td>`300px`</td>
</tr>
<tr>
<td>`--#${prefix}hero-overlap-negative-spacing`</td>
<td>per cambiare l'altezza di overlap del contenuto successivo </td>
<td>`-3rem`</td>
</tr>
<tr>
<td>`--#${prefix}hero-overlap-spacing-inset`</td>
<td> </td>
<td>`48px`</td>
</tr>

<tr>
<td>`--#${prefix}hero-overlay-background`</td>
<td> </td>
<td>`transparent`</td>
</tr>

<tr>
<td>`--#${prefix}hero-text-family`</td>
<td>per cambiare il font-family del testo</td>
<td>`var(--bsi-font-serif)`</td>
</tr>

<tr>
<td>`--#${prefix}hero-text-color`</td>
<td>per cambiare il colore del testo</td>
<td>`#fff`</td>
</tr>

</tbody>
</table>

[Vai alla guida sul selettore part](/docs/personalizzazione-degli-stili--documentazione#selettore-part)
