import { Meta, Story, Canvas, Controls, Description, Subtitle } from '@storybook/addon-docs/blocks';
import * as HeroStories from './it-hero.stories';

<Meta of={HeroStories} />

# Hero

<description>Sezione a tutta larghezza per mettere in evidenza combinazioni di contenuti testuali, immagini e pulsanti</description>


## Cosa fa

Il componente Hero puoi usarlo per guidare l'utente verso un contenuto in evidenza e una call to action, oppure come apertura di grande impatto visivo della pagina che possa invogliare gli utenti a scorrere ulteriori contenuti.

[Approfondisci quando e come usare il componente Hero](https://designers.italia.it/design-system/componenti/hero/)


## Anteprima e attributi del componente

Modifica gli attributi nella tabella per personalizzare in tempo reale l’aspetto e il comportamento del componente. Per vedere come cambia il codice, clicca su <strong>Show code</strong>.

<Canvas of={HeroStories.EsempioInterattivo} />
<Controls of={HeroStories.EsempioInterattivo} />

## Indicazioni generali

Puoi facoltativamente estendere la larghezza del componente Hero all’intera finestra, per mostrare messaggi in evidenza sul tuo sito.

### Attenzione a
- Non usare più hero sulla stessa pagina.
- Usa il componente Hero con moderazione, consideralo come fosse una copertina o un banner molto in evidenza. Difficilmente avrai bisogno di troppe copertine o banner nell'ambito dello stesso sito o servizio.
- Fai attenzione al corretto contrasto (minimo) tra testo e sfondo nella variante "Con testo e immagine di sfondo"
- Scegli il corretto livello di heading `h1`-`h6` per il titolo a seconda del contesto e della struttura di pagina.
- Usa la corretta semantica html per le call to action laddove presenti: `<a>` per link di navigazione o ancore, `<it-button>` per azioni.
- Quando usi il componente Hero con approccio "fullwidth", sfrutta tutta la larghezza della viewport.

## Accessibilità

- Valuta attentamente l'utilizzo di più hero nella stessa pagina.
- Nel caso in cui utilizzi il componente Hero con sola immagine, imposta sempre l'attributo `it-aria-label` sul tag `<it-hero>` per annunciare tramite i lettori di schermo di cosa si tratta.
- Nel caso in cui inserisci contenuti testuali, utilizza per il titolo il livello `h` corretto a seconda del contesto.
- Nel caso in cui utilizzi il componente Hero nella versione con testo e immagine di sfondo, verifica che l’immagine utilizzata assicuri sempre un adeguato contrasto rispetto al testo.

## Con immagine
Per mostrare una immagine di sfondo nel componente `it-hero`, inserisci l'immagine nello slot `background` dedicato.

Ricordati di impostare l'attributo `it-aria-label` sul tag `<it-hero>` per annunciare tramite lettori di schermo di cosa si tratta.

<Canvas of={HeroStories.ConImmagine} />

## Con contenuti testuali
Inserisci il testo nello slot `text` dedicato.
<div class="callout callout-success">
<div class="callout-inner mt-0">
<div class="callout-title">
<span class="text">Accessibilità contenuti testuali</span>
</div>
<p>
Nei casi in cui fossero presenti contenuti testuali, utilizza per il titolo il livello `h` corretto a seconda del contesto.
</p>
</div>
</div>

<Canvas of={HeroStories.ConContenutiTestuali} />

### Con contenuti testuali centrati
Per ottenere il componente Hero con testi centrati orizzontalmente, aggiungi l'attributo `center` al tag `<it-hero>`.

<Canvas of={HeroStories.ConContenutiTestualiCentrati} />

## Con testi ed immagine di sfondo

Per visualizzare i testi in overlay sulle immagini, viene creato automaticamente un div scuro in opacità per migliorare la leggibilità del testo in caso di immagini dai toni chiari.

<div class="callout callout-success">
<div class="callout-inner mt-0">
<div class="callout-title">
<span class="text">Accessibilità e contrasto</span>
</div>
<p>
Verifica che l’immagine utilizzata assicuri sempre un adeguato contrasto rispetto al testo.
</p>
</div>
</div>

<Canvas of={HeroStories.ConTestoEImmagine} />

### Con overlay di colore primario
Per un div opaco di colore primario, aggiungi l'attributo `overlay-color="primary"` al tag `<it-hero>`.

<Canvas of={HeroStories.ConTestoEImmaginePrimary} />


### Con overlay e filtro di colore primario
Per aggiungere un filtro di tipo “screen”, aggiungi l'attributo `overlay-color="filter"` al tag `<it-hero>`.

<Canvas of={HeroStories.ConTestoEImmaginePrimaryFilter} />


## Con immagine e margine negativo per contenuti sovrapposti
In alcuni casi, il contenuto che segue può sovrapporsi al componente Hero.

Aggiungi l'attributo `overlap` al tag `<it-hero>` per far sì che il contenuto seguente si sovrapponga al componente Hero (in questo caso il componente seguente è una card).

<Canvas of={HeroStories.OverlapContent} />

## Personalizzazione degli stili

Per la personalizzazione degli stili puoi usare i selettori `::part` dedicati:

- `::part(img-responsive-wrapper)` - wrapper dell'immagine di sfondo
- `::part(text-container)` - contenitore dello slot del testo

Puoi cambiare il valore delle seguenti variabili css per:
<table>
<thead>
<tr>
<td>Variabile CSS</td>
<td>Descrizione </td>
<td>Predefinito</td>
</tr>
</thead>
<tbody>
<tr>
<td>`--#${prefix}hero-background-color`</td>
<td>per cambiare il colore di sfondo dell'hero </td>
<td>`var(--#${prefix}color-background-primary)`</td>
</tr>
<tr>
<td>`--#${prefix}hero-car-size`</td>
<td></td>
<td>`1rem`</td>
</tr>
<tr>
<td>`--#${prefix}hero-height`</td>
<td>per cambiare l'altezza dell'hero </td>
<td>`300px`</td>
</tr>
<tr>
<td>`--#${prefix}hero-overlap-negative-spacing`</td>
<td>per cambiare l'altezza di overlap del contenuto successivo </td>
<td>`-3rem`</td>
</tr>
<tr>
<td>`--#${prefix}hero-overlap-spacing-inset`</td>
<td> </td>
<td>`48px`</td>
</tr>

<tr>
<td>`--#${prefix}hero-overlay-background`</td>
<td> </td>
<td>`transparent`</td>
</tr>

<tr>
<td>`--#${prefix}hero-text-family`</td>
<td>per cambiare il font-family del testo</td>
<td>`var(--bsi-font-serif)`</td>
</tr>

<tr>
<td>`--#${prefix}hero-text-color`</td>
<td>per cambiare il colore del testo</td>
<td>`#fff`</td>
</tr>

</tbody>
</table>

[Vedi qui la guida dettagliata](/docs/personalizzazione-degli-stili--documentazione#selettore-part).
