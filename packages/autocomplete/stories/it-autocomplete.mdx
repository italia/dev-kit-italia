import { Meta, Story, Canvas, Controls, Description, Subtitle, Title } from '@storybook/addon-docs/blocks';
import * as Stories from './it-autocomplete.stories';

<Meta of={Stories} />

# Autocomplete

<description>Campo di input che suggerisce automaticamente opzioni di completamento man mano che l'utente digita.</description>

## Cosa fa
Il componente Autocomplete (`it-autocomplete`) suggerisce opzioni di autocompletamento mentre l'utente digita in un campo input. I suggerimenti si basano sul testo inserito e aiutano l'utente a compilare rapidamente il campo senza dover scrivere il testo per intero.

[Approfondisci quando e come usare il componente Autocomplete](https://designers.italia.it/design-system/form/autocompletamento/)

## Anteprima e attributi del componente
Modifica gli attributi nella tabella per personalizzare in tempo reale l'aspetto e il comportamento del componente.
Per vedere come cambia il codice, clicca su **Show code**.

<Canvas of={Stories.EsempioInterattivo} />
<Controls of={Stories.EsempioInterattivo} />


## Accessibilità

Il componente implementa il pattern [ARIA Combobox with List Autocomplete](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-autocomplete-list/)
che garantisce:
- corretti attributi ARIA per la navigazione assistita;
- annunci vocali per lettori di schermo;
- gestione completa della navigazione da tastiera;
- appropriata gestione del focus.

Includi sempre un’etichetta descrittiva del campo nello slot `label`.

## Testo segnaposto

Per aggiungere un testo segnaposto con esempi di compilazione, usa l'attributo `placeholder`.

<Canvas of={Stories.Placeholder} />

## Testo di supporto

Per aggiungere un testo con istruzioni di compilazione, usa l'attributo `support-text`. Il testo di supporto apparirà sotto il campo input e resterà sempre visibile.

<Canvas of={Stories.TestoDiSupporto} />

## Campo obbligatorio

Per rendere il campo obbligatorio, usa l'attributo `required`.

<Canvas of={Stories.CampoObbligatorio} />

## Lunghezza minima per i suggerimenti

Per impostare un numero minimo di caratteri da digitare prima che vengano mostrati i suggerimenti, usa l'opzione `min-length`. Questo è particolarmente utile quando si lavora con grandi quantità di dati.

<Canvas of={Stories.ConMinLength} />

## Stato disabilitato

Per impedire l'interazione con il campo, usa l'attributo `disabled`.
<Canvas of={Stories.Disabilitato} />


## Etichetta nascosta

Per nascondere l'etichetta (ad esempio nei campi di ricerca), usa l'attributo `label-hidden`.

L'etichetta sarà nascosta visivamente ma verrà letta dai lettori di schermo.

<Canvas of={Stories.LabelHidden} />

## Opzioni dinamiche

Puoi far popolare dinamicamente le opzioni del componente `it-autocomplete` attraverso manipolazioni JavaScript e/o definendo una funzione da associare all'attributo `source` via JS. Il formato delle opzioni deve essere un array di oggetti con le proprietà `value` e `label`.

### Dati dipendenti da un altro input

Puoi impostare che le opzioni suggerite dal componente si aggiornino dinamicamente in base al valore che è stato selezionato in un altro campo input, come ad esempio una select di un form.

Espandi il codice degli esempi per scoprire come implementare questa funzionalità.

<Canvas of={Stories.DatiDinamici} />
<Canvas of={Stories.RegioniEComuni} />

### Ricerca asincrona

Puoi passare come opzione `source` una funzione per filtrare i dati e popolare il componente dinamicamente.
La funzione riceve come parametri la query di ricerca e una callback per popolare i risultati.

```ts
source: AutocompleteOption[]
  | ((
        query: string,
        callback: (results: AutocompleteOption[]) => void
      ) => void)
```


Espandi il codice dell'esempio per scoprire come implementare questa funzionalità.

<Canvas of={Stories.RicercaAsincrona} />

## Valore di default
Per impostare un valore iniziale, usa l'attributo `default-value`.

Impostare un valore iniziale richiede un approccio diverso a seconda di come carichi i dati relativi alle opzioni (`source`):

1. Per **opzioni statiche**, **usa una stringa**. Se passi un array statico a `source`, imposta l'attributo `default-value` con il valore stringa del `value` corrispondente. Il componente troverà da solo l'etichetta corretta da mostrare.

2. Per **opzioni dinamiche**, **usa un oggetto completo**. Se usi una funzione per caricare i dati nell'attributo `source`, il componente potrebbe non conoscere le opzioni all'avvio. Popola l'attributo `default-value` con un oggetto che contenga sia `value` che `label`.

Se stai usando il componente in un framework (React, Angular, Vue, Svelte), segui le indicazioni del tuo framework per quanto riguarda gestione eventi, lifecycle events e fetching asincrono dei dati.

Per scoprire i diversi comportamenti, clicca su Show code negli gli esempi.
<Canvas of={Stories.MatriceComportamento} />

## {Stories.MetodiEPropPubblici.name}
<Description of={Stories.MetodiEPropPubblici}/>

## {Stories.I18n.name}
<Description of={Stories.I18n}/>
