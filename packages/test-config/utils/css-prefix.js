// Single source of truth for the default Bootstrap Italia CSS custom
// property prefix (bootstrap-italia/src/scss/base/_config.scss: `$prefix`),
// so tests reading `--{prefix}...` custom properties don't hardcode it.
export const CSS_PREFIX = 'bsi-';
