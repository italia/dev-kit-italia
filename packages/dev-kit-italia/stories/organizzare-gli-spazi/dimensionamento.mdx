import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as Stories from './dimensionamento.stories';

<Meta of={Stories} />

# Dimensionamento

## Larghezza fissa

Le grandezze di default presenti sono: `25%`, `50%`, `75%` e `100%`.

<Canvas of={Stories.LarghezzaFissa} />

## Altezza fissa
Le altezze di default presenti sono: `25%`, `50%`, `75%` e `100%`.

<Canvas of={Stories.AltezzaFissa} />

## Larghezza massima

Sono previste anche le classi `mw-100` e `mh-100` per i corrispettivi `max-width: 100%;` e `max-height: 100%;`.

<Canvas of={Stories.LarghezzaMassima} />

<Canvas of={Stories.AltezzaMassima} />
