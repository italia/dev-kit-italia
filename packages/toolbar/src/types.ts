export const TOOLBAR_SIZES = ['lg', 'md', 'sm'];
export const TOOLBAR_ORIENTATIONS = ['horizontal', 'vertical'];

export type ToolbarSizes = (typeof TOOLBAR_SIZES)[number] | '';
export type ToolbarOrientations = (typeof TOOLBAR_ORIENTATIONS)[number] | '';
