export type CarouselVariants = 'single' | 'columns' | 'gallery-sm' | 'gallery-lg';

/** Splide slide movement type. Mirrors Splide's own `type` option for type safety. */
export type CarouselType = 'slide' | 'loop' | 'fade';
