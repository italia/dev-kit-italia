import { Meta, Story, Canvas, Controls, Description, Subtitle, Title } from '@storybook/addon-docs/blocks';
import * as Stories from './it-carousel.stories';

<Meta of={Stories} />

# Carousel

<description>Componente di presentazione per scorrere orizzontalmente una sequenza di elementi, immagini o diapositive di testo.</description>

## Cosa fa
Il componente Carousel (`it-carousel`) mostra contenuti correlati che non possono essere visualizzati tutti insieme, permettendo di sfogliarli orizzontalmente.

È utile per eventi, notizie o servizi in evidenza, ma evita di usarlo per contenuti critici, perché alcuni utenti potrebbero non notarli.

[Approfondisci come e quando usare il componente Carousel](https://designers.italia.it/design-system/componenti/carousel/)

## Anteprima e attributi del componente

Nella tabella trovi gli attributi per personalizzare l'aspetto e il comportamento del componente. Data la complessità del componente, l'anteprima non verrà aggiornata in tempo reale al variare degli attributi.

Per comprendere appieno le funzionalità del componente e delle sue varianti, consulta gli esempi interattivi contenuti nelle altre sezioni di questa pagina.

<Canvas of={Stories.EsempioInterattivo} />
<Controls of={Stories.EsempioInterattivo} />

## Indicazioni generali
Il componente `it-carousel` consente di creare una sequenza orizzontale di qualsiasi elemento HTML (card, immagini, video, ecc.).

Per implementarlo correttamente:
- usa il tag `<it-carousel>` come wrapper principale;
- inserisci ogni elemento "slide" all'interno di un elemento `<it-carousel-slide>`, nello slot di default;
- fornisci un'intestazione nello slot `title` usando un elemento `h` (`h2`–`h6`).

Il componente espone i seguenti attributi:
- `variant`, per selezionare la variante di layout;
- `arrows`, per mostrare le frecce di navigazione;
- `fullscreen`; per visualizzare il carousel a tutto schermo;
- `type`, per controllare il comportamento di scorrimento. Se non impostato viene usato il valore di default della variante;
- `autoplay`, abilita il comportamento Play/Pause. Di default è in modalità `pause` e viene mostrato un pulsante Play/Pausa per l'utente.
- `config`, oggetto per personalizzazioni avanzate che estende le opzioni offerte da Splide, passato come stringa JSON nell'attributo.

## Accessibilità
Il componente `it-carousel` integra Splide.js e segue le specifiche [W3C Carousel Design Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/). Per tutte le funzionalità di accessibilità offerte dalla libreria, [consulta la documentazione di Splide](https://splidejs.com/guides/accessibility).

- Il wrapper principale usa `role="region"` per permettere agli utenti di lettori di schermo di individuare e saltare rapidamente alla sezione.
- Puoi assegnare un nome accessibile automaticamente: inserendo un elemento `h` (`h2`-`h6`) nello slot `title`. Il componente genera un ID univoco e lo collega al carousel tramite `aria-labelledby`. Se vuoi nascondere l'intestazione visivamente, puoi utilizzare la classe `visually-hidden` sull'elemento di intestazione.
- Tutti le slide non visibili hanno l'attributo `inert`, così gli utenti che navigano con la tastiera non finiscono su elementi nascosti.
- I controlli (frecce e paginazione) sono realizzati con elementi `<button>` nativi. Durante la transizione, il componente rimuove preventivamente le restrizioni di focus dalla slide di destinazione per permettere alle tecnologie assistive di seguirne il contenuto.
- Con autoplay attivo, viene fornito un pulsante Play/Pausa. Per impostazione predefinita, il carousel è in modalità pausa: movimento e animazioni sono disabilitati durante hover, focus o se l'utente ha scelto di ridurre le animazioni nelle impostazioni del sistema operativo o del browser.
- Grazie all'estensione [Intersection](https://splidejs.com/extensions/intersection/), l’autoplay si mette in pausa quando il carousel esce dal campo visivo dell’utente, risparmiando risorse e riducendo il rumore cognitivo.

Per garantire un'esperienza utente ottimale ed accessibile:
- non usare il carousel come unica modalità di navigazione sul tuo sito o servizio;
- assicurati che il carousel non intralci l’esperienza dell’utente se viene ignorato o non utilizzato;
- assicurati che il carousel non interferisca con altri elementi interattivi sulla pagina;
- imposta una velocità di transizione adeguata per permettere alle persone di assimilare le informazioni;
- verifica che le immagini o i contenuti siano di alta qualità, chiaramente visibili su tutti i dispositivi e che includano testo alternativo descrittivo;
- fornisci sempre un'intestazione chiara e descrittiva dello scopo e dei contenuti del carousel. Se l'intestazione è puramente decorativa, nascondila visivamente usando la classe `visually-hidden`.

## Varianti
Il componente presenta diverse varianti, selezionabili tramite l'attributo `variant`.

### Variante singola
La variante `single` mostra un singolo elemento alla volta e include frecce di scorrimento.
<Canvas of={Stories.VarianteSingola} />

### Variante a tre colonne
La variante `columns` mostra più elementi contemporaneamente, organizzati in colonne.

<Canvas of={Stories.Variante3Colonne} />

### Varianti galleria
Le varianti galleria presentano una serie di immagini o contenuti visivi in un formato di galleria.

- La variante `gallery-sm` offre una visualizzazione più compatta.
- La variante `gallery-lg` offre una visualizzazione più ampia e immersiva.

<Canvas of={Stories.VarianteGallerySM} />
<Canvas of={Stories.VarianteGalleryLG} />


## Aggiungere un separatore tra le slide

Per aggiungere un separatore verticale tra le slide del carousel, usa l'attributo `separator`. Questa opzione aiuta a distinguere visivamente gli elementi, soprattutto quando il contenuto delle slide è simile o quando si utilizzano varianti che mostrano più elementi contemporaneamente (come `columns`).
<Canvas of={Stories.Separator} />

## Frecce di navigazione
Le frecce di navigazione facilitano lo scorrimento tra gli elementi. Per abilitarle, usa l'attributo `arrows`.

Le frecce di navigazione vengono abilitate o disabilitate dinamicamente in base alla posizione corrente all'interno del carosello (ad esempio, la freccia "precedente" è disabilitata quando si è sulla prima slide), al tipo di scorrimento e alla variante selezionata.


<Canvas of={Stories.ConFrecce1} />

## Fullscreen
Per visualizzare il componente a tutto schermo, usa l'attributo `fullscreen`.
Questa opzione è particolarmente utile per le varianti galleria, ma può essere applicata a qualsiasi variante per creare un'esperienza immersiva.
<Canvas of={Stories.Fullscreen} />

## Modificare il comportamento di scorrimento
Per modificare il comportamento di scorrimento, valorizza l'attributo `type` con:

- `slide`, per spostare le slide lateralmente, partendo dalla prima e fermandosi all'ultima;
- `loop`, per uno scorrimento circolare continuo, senza fermarsi all'ultima slide;
- `fade`, per una transizione più morbida con dissolvenza.

Per transizioni più avanzate (durata, easing, comportamento su breakpoint, ecc.), passa un oggetto compatibile con Splide tramite l'attributo `config`. Per tutte le opzioni disponibili, consulta la [documentazione ufficiale di Splide](https://splidejs.com/guides/options/#type).

<Canvas of={Stories.TipoScorrimento} />

## Autoplay
Per far scorrere automaticamente le slide dopo un intervallo di tempo specificato, usa l'attributo `autoplay`.

Per impostazione predefinita, il carousel è in modalità pausa: movimento e animazioni sono disabilitati durante hover, focus o se l'utente ha scelto di ridurre le animazioni nelle impostazioni del sistema operativo o del browser.

Il tempo di intervallo è impostato a 5000 ms (5 secondi) di default. Per personalizzarlo, usa l'attributo `config`, passando un oggetto compatibile a Splide con la proprietà `interval` espressa in millisecondi.

<Canvas of={Stories.Autoplay} />

## Personalizzazione avanzata attraverso l'attributo `config`

Per personalizzare ulteriormente stile e comportamento del carousel (ad esempio velocità di scorrimento, numero di elementi visualizzati per pagina, funzionalità avanzate dei breakpoint), usa l'attributo `config` per passare un [oggetto di configurazione conforme alle opzioni di Splide](https://splidejs.com/guides/options).

In questo esempio, il carousel è configurato per mostrare 2 elementi per pagina, con autoplay e loop attivi e un intervallo di 3 secondi tra le transizioni.

<div class="callout callout-warning">
  <div class="callout-inner">
    <div class="callout-title">
      <span class="text">Accessibilità</span>
    </div>
    <p>
      Questo esempio ha scopo illustrativo e potrebbe non essere adatto per tutti i contesti o esigenze di accessibilità.
    </p>

  </div>
</div>

```html
<it-carousel variant="columns" config="{
      type: 'loop',
      autoplay: 'pause',
      interval: 3000,
      pauseOnHover: true,
      resetProgress: false,
      perPage: 2,
      arrows: true,
      gap: 16,
      padding: { left: 8, right: 8 },
      breakpoints: {
        560: {
          perPage: 1,
          gap: 0,
          padding: { left: 24, right: 24 },
          arrows: false,
        },
        768: {
          perPage: 2,
          gap: 0,
          padding: { left: 0, right: 0 },
          arrows: false,
        },
        992: {
          perPage: 3,
          gap: 0,
          padding: { left: 0, right: 0 },
          arrows: false,
        },
      },
    }">
  <!-- Contenuto del Carousel -->
</it-carousel>
```

<Canvas of={Stories.ConfigurazioneAvanzata} />

## Proprietà, metodi ed eventi accessibili via JS

Il componente espone i seguenti metodi e eventi accessibili via JavaScript.

### Metodi
- `playAutoplay()`, avvia l'autoplay (se configurato).
- `pauseAutoplay()`, mette in pausa l'autoplay.

### Eventi
- `it-carousel-play`, emesso quando l'autoplay viene avviato. Evento configurato con `bubbles: true` e `composed: true`. Nel `detail` contiene `{ id: string }` (id della istanza Splide).
- `it-carousel-pause`, emesso quando l'autoplay viene messo in pausa. Evento configurato con `bubbles: true` e `composed: true`. Nel `detail` contiene `{ id: string }`.

## {Stories.I18n.name}
<Description of={Stories.I18n}/>


## Personalizzazione degli stili
Per personalizzare l'aspetto di `it-carousel` dall'esterno dello shadow DOM, usa il selettore `::part()` sui seguenti part esposti dal componente:

- `arrows`, per il wrapper che contiene i pulsanti freccia (prev/next);
- `arrow-prev`, per il pulsante "precedente";
- `arrow-next`, per il pulsante "successivo";
- `pagination-track`, per il contenitore della paginazione (lista di dot);
- `pagination-dot`, per ogni singolo dot/pulsante della paginazione;
- `autoplay-toggle`, per il toggle Play/Pausa peresente quando `autoplay` è attivo o `config.autoplay` è configurato;
- `autoplay-play`, per l'icona SVG di Play all'interno del toggle;
- `autoplay-pause`, per l'icona SVG di Pause all'interno del toggle.

[Vai alla guida sul selettore part](/docs/personalizzazione-degli-stili--documentazione#selettore-part)
